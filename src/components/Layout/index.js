import React from 'react';
import { Container } from 'reactstrap';

const Layout = (props) => {
    return (
        <Container fluid >
            Test layout
        </Container>
    );
}

export default Layout;